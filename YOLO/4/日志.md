## 在hugging face上面下载模型
1. 可以点击左侧栏里的text generation ，因为这里要找的是交互式对话的模型嘛，筛选自己想要的类别。  
找的话可以以trending（热度）排序。
2. 如何下载，使用教程之类，点进模型的介绍里面都有。  
3. gpt2模型有目前见到的有GPT2LMHeadModel 和  GPT2Model 两个。后者没有generator的功能，也就是说并不能生成对话，只能提取文本特征  
但是如果使用pipline的方式导入的话，其实没有指定用哪一个，但是可以使用generator  ！！！ 要问明白  
豆包：在 Hugging Face 的 Transformers 库中，当你使用pipeline来创建文本生成任务时，它会根据任务类型自动选择合适的模型类。  
对，pipline里面还写了一个text generator。这个应该是网页上面左边的分类。
4. 其他重要的都直接在写的时候附进ipynb文件里面了

## 对初始模型进行调教（就是使用，还没有微调）
1. 发现有Kobe Bryant:Kobe Bryant: "Bryant is a great player,；Kobe Bryant:Michael Jordan:这样的输出格式，
加一个replace把重复的东西删掉就好了。

## 开始微调！
1. 首先因为像CV领域的微调都是保留encoder，修改decoder，所以我觉的这里也是要采用类似的手段。那么就看下有那几层。
具体操作见ipynb文件。
2. 我发现这里model上面的格式和我用nn,module得到的模型的格式好像。原来可以直接用一模一样的方法train一发。  
3. 很奇怪，我给他文本数据进行微调的时候，模型效果反而越调整越差了，甚至会输出一些莫名其妙的网址（但是我的微调数据没有网址，都是对话），或者输出为空。  
一开始怀疑是不是因为给的数据是Kobe：...  ,而我的prompt是Kobe Bryant:.... 导致训练异常，但是改了之后完全没有效果
4. 问豆包修改了label与input对应关系的错误，但是模型在微调后仍然表现得很差  
5. 感觉可能问ai也不能解决问题，而且问ai问到的都是一些零碎的知识，所以索性去hugging face官网上看docs了。先看transformers-training-finetuning。  
摘录：Trainer is an optimized training loop for Transformers models, making it easy to start training right away without manually writing your own training code. Pick and choose from a wide range of training features in TrainingArguments such as gradient accumulation, mixed precision, and options for reporting and logging training metrics.  
人麻了。搞半天原来hf自带微调用的函数，根本不用我自己去仿照这nn.modele来写  
6. 